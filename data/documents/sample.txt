Retrieval-Augmented Generation (RAG)

RAG is a technique that combines information retrieval with large language model generation.
Instead of relying solely on the LLM's parametric knowledge, RAG fetches relevant documents
from an external knowledge base at inference time and injects them into the prompt.

Architecture Overview

A typical RAG system has three stages:
1. Indexing — documents are split into chunks, embedded with a dense vector model,
   and stored in a vector database such as Qdrant or Pinecone.
2. Retrieval — at query time the question is embedded with the same model, and the
   k nearest neighbours are fetched from the vector store using approximate nearest
   neighbour (ANN) search.
3. Generation — the retrieved chunks are formatted as context and passed together
   with the question to the language model, which produces a grounded answer.

Qdrant

Qdrant is an open-source vector database written in Rust. It supports:
- Dense and sparse vector search
- Payload filtering (metadata filters applied during ANN search)
- GRPC and HTTP APIs
- On-disk and in-memory storage backends
- Horizontal scaling via sharding and replication

Qwen Language Models

Qwen is a family of large language models released by Alibaba DAMO Academy.
Key variants include Qwen1.5-4B-Chat (4 billion parameters) and Qwen2.5 in sizes
ranging from 0.5B to 72B. All chat variants use the ChatML prompt format with
special tokens <|im_start|> and <|im_end|>. The models support a context window
of up to 32,768 tokens in the 1.5/2/2.5 series.

Embedding Models

For multilingual retrieval Octen-Embedding-0.6B is a strong lightweight choice. It is based on
Qwen3-Embedding-0.6B, produces 1024-dimensional L2-normalised vectors, and supports
100+ languages. Query embeddings can use the model's retrieval prompt for better
semantic search quality.

Chunking Strategies

Optimal chunk size balances context richness against retrieval precision.
Typical ranges:
- 128–256 tokens: high precision, low context
- 512 tokens: good default for paragraph-level reasoning
- 1024+ tokens: richer context but noisier retrieval

Overlap between consecutive chunks (50–100 tokens) prevents information loss at
chunk boundaries.
