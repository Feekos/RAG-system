Retrieval-Augmented Generation (RAG) — основы

RAG (Retrieval-Augmented Generation, или Генерация с извлечением) — это метод, который сочетает поиск информации из внешней базы знаний с генерацией текста языковой моделью. Вместо того чтобы полагаться только на параметрические знания LLM, RAG в момент запроса извлекает релевантные документы и передаёт их в промпт, обеспечивая обоснованный ответ.

Архитектура RAG-системы

Типичная RAG-система состоит из трёх этапов:

1. Индексация (Indexing) — документы разбиваются на фрагменты (чанки), кодируются с помощью модели векторных представлений (embedding-модели) и сохраняются в векторной базе данных, например Qdrant или Pinecone.

2. Извлечение (Retrieval) — в момент поступления запроса он также кодируется той же embedding-моделью, после чего из векторного хранилища извлекаются k ближайших соседей с помощью приближённого поиска ближайшего соседа (ANN).

3. Генерация (Generation) — извлечённые фрагменты форматируются в контекст и передаются вместе с вопросом языковой модели, которая формирует обоснованный ответ.

Qdrant как векторная база данных

Qdrant — это высокопроизводительная векторная база данных с открытым исходным кодом, написанная на Rust. Она поддерживает:
- Поиск по плотным (dense) и разреженным (sparse) векторам
- Фильтрацию по метаданным (payload-фильтры), применяемую прямо во время ANN-поиска
- Горизонтальное масштабирование с помощью шардирования и репликации
- HTTP и gRPC API
- Дисковое и in-memory хранение

Qwen — семейство языковых моделей

Qwen — семейство больших языковых моделей, разработанных в Alibaba DAMO Academy. Модели поддерживают русский язык и другие языки помимо английского и китайского. Ключевые варианты:
- Qwen1.5-4B-Chat — модель с 4 миллиардами параметров, поддерживает диалоговый режим
- Qwen2.5 — улучшенная серия от 0,5B до 72B параметров
- Все диалоговые варианты используют формат ChatML с токенами <|im_start|> и <|im_end|>
- Контекстное окно — до 32 768 токенов

Embedding-модели для мультиязычного поиска

Для мультиязычного извлечения информации, включая русский язык, хорошим выбором является Octen-Embedding-4B. Она:
- Основана на Qwen3-Embedding-4B
- Производит векторы размерностью 2560
- Поддерживает более 100 языков, включая русский
- Нормирует векторы по L2, что позволяет использовать скалярное произведение вместо косинусного сходства
- Поддерживает специальный query prompt для асимметричного поиска (запрос против абзаца)

Разбивка текста на фрагменты

Оптимальный размер фрагмента балансирует между точностью извлечения и полнотой контекста:
- 128–256 символов — высокая точность, мало контекста
- 512 символов — хороший баланс для рассуждений на уровне абзаца
- 1024+ символов — больше контекста, но шумнее при извлечении

Перекрытие между соседними фрагментами (50–100 символов) предотвращает потерю информации на границах фрагментов.

Ключевые преимущества RAG

По сравнению с дообучением (fine-tuning) RAG имеет следующие преимущества:
- Знания можно обновлять без переобучения модели
- Более низкая стоимость при добавлении новых документов
- Воспроизводимые ответы с возможностью цитирования источников
- Уменьшение галлюцинаций за счёт заземления на реальные документы
